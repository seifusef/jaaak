/// Temporary feature flags for prototype features.
///
/// Flip [kEnableIndoPakMushaf] to `false` to hide the IndoPak Mushaf entry
/// point without removing any code.
const bool kEnableIndoPakMushaf = true;
